<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agridronee">

    <!-- ✅ Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- You had INTERNET permission listed twice, I removed the duplicate -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- ✅ Bluetooth Permissions (For Devices Running Android 12 and Above) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AgriDronee"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agridronee"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">  <!-- Added this line -->

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="XXX" /> <!-- add your own api -->

        <!-- ✅ LOGIN SCREEN (App Entry) -->
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SurveyActivity"
            android:label="Survey Reports"
            android:exported="false" />

        <!-- ✅ MAIN ACTIVITY -->
        <activity android:name=".MainActivity"
            android:exported="true"/>

        <activity android:name=".LiveMap"
            android:exported="true" />

        <!-- ✅ SETTINGS SCREEN (With UI Mode Config Change for Dark Mode) -->
        <activity android:name=".SettingsActivity"
            android:exported="true"
            android:configChanges="uiMode"/>

        <!-- ✅ PROFILE & REGISTRATION -->
        <activity android:name=".ProfileActivity"
            android:exported="true"/>
        <activity android:name=".EditProfileActivity"
            android:exported="true"/>
        <activity android:name=".RegisterActivity"
            android:exported="true"/>

        <activity android:name=".FlightControlActivity"/>

        <activity android:name=".FlightLogsActivity"
            android:exported="true">
            <!-- Move the intent-filter inside the FlightLogsActivity -->
            <!-- Only needed if you want this activity to also be launchable from the app drawer -->
            <!-- Note: You probably don't want two launcher activities, remove if not needed -->
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        <!-- ✅ WEBVIEW ACTIVITY -->
        <activity android:name=".WebViewActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize"
            android:hardwareAccelerated="true"/>

    </application>
</manifest>